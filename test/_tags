true: \
  debug, \
  safe_string, \
  warn(A-44), \
  package(visitors.ppx), \
  package(visitors.runtime)

<expr07*> or <expr08*> or <expr14*>: \
  package(hashcons)

<bench.*>: \
  thread, \
  package(core_bench)

<expr_import*>: \
  package(ppx_import)
